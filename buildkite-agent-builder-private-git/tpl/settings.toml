#
# This is the configuration file for BuildKite Agent
[agent_token]
token = "YOUR BUILDKITE TOKEN"

[ssh]
id_rsa = "copy YOUR_SSH_PRIVATE_KEY encoded with base64- e.g.: echo -n "$(cat id_rsa)" | base64"
